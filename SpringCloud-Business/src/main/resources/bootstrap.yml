server:
  port: 8080
spring:
  application:
    name: springcloud-business
  cloud:
    nacos:
      config:
        server-addr: 114.132.190.31:8848
        prefix: ${spring.application.name}
        file-extension: yml