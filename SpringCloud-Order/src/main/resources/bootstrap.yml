server:
  port: 8383
spring:
  application:
    name: springcloud-order
  cloud:
    nacos:
      config:
        server-addr: 114.132.190.31:8848
        prefix: ${spring.application.name}
        file-extension: yml